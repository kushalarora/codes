#include<string.h>
#include<iostream>
#include<cstdlib>
#include<ctime>
#include<string>
#include<math.h>
#include<map>
using namespace std;
const bool DEBUG = true;

#define REP(i, B, E) for(i = B; i < E; i++)
template<class T>
inline void write (const T& a) {if (DEBUG) cout<<a<<" ";}
template<class T>
inline void  write2(const T& a, const T& b){ if (DEBUG) cout<<a<<" "<<b<<"\n";}
#define RESET_MEM(pointer, size) memset(pointer, 0, size)
const int ASCII_A = 65;
const int ASCII_Z = 91;
const int ASCII_a = 97;
const int ASCII_z = 122;

int main() {
    write(10);
    return 0;
}
